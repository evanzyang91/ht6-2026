import { it } from "vitest";

// Success criterion: produce at least three Conventions, each supported by multiple real PRs.
it.todo("buildConventions() produces >= 3 conventions backed by multiple PRs each");
it.todo("scoreLinkageQuality() returns 'unknown' when no accepted fix is found");
