// Bootstraps the MCP server (stdio transport) and registers the tool handlers from
// ./tools/index.ts. Protocol wiring details TBD — see @modelcontextprotocol/sdk docs.
//
// TODO: instantiate McpServer, register get_repo_conventions and predict_review_feedback,
// connect a stdio transport, and listen.

async function main(): Promise<void> {
  throw new Error("not implemented");
}

main();
