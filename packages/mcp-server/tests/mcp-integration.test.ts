import { it } from "vitest";

// Success criterion: an MCP client can retrieve real conventions and validate a supplied
// diff against them, end to end (spin up the server, call both tools over the MCP protocol).
it.todo("MCP client can call get_repo_conventions and predict_review_feedback end-to-end");
