// TODO: fetch 50-100 merged PRs for a repo (metadata: number, title, author, mergedAt,
// mergeCommitSha, etc). Use paginate.ts + rateLimit.ts.
export async function fetchMergedPullRequests(owner: string, repo: string) {
  throw new Error("not implemented");
}
