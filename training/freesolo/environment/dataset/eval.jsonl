{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/api\",\"pullRequest\":310,\"filePath\":\"src/routes/reports.ts\",\"reviewComment\":\"New public endpoints must be guarded by a feature flag.\",\"rejectedCode\":\"router.get('/reports', requireAuth, reportsController)\",\"acceptedCode\":\"router.get('/reports', requireAuth, requireFeature('reports'), reportsController)\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  router.get('/reports', requireAuth, reportsController)\\n}\",\"acceptedContext\":\"function reviewedSymbol() {\\n  router.get('/reports', requireAuth, requireFeature('reports'), reportsController)\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"architecture\",\"title\":\"Feature-flag new public endpoints\",\"rule\":\"New public endpoints must be protected by the appropriate feature flag.\",\"rationale\":\"The accepted route adds the reports feature gate before the controller.\",\"detection\":{\"mode\":\"missing-required-signal\",\"semanticDescription\":\"A new public endpoint is defined without its feature gate.\",\"triggerSignals\":[\"reportsController\"],\"forbiddenSignals\":[],\"requiredSignals\":[\"requireFeature\"],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/api\",\"pullRequest\":318,\"filePath\":\"src/services/checkout.ts\",\"reviewComment\":\"These writes need to happen in one transaction.\",\"rejectedCode\":\"await orderRepository.save(order); await paymentRepository.save(payment)\",\"acceptedCode\":\"await database.transaction(async (tx) => { await orderRepository.save(order, tx); await paymentRepository.save(payment, tx) })\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  await orderRepository.save(order); await paymentRepository.save(payment)\\n}\",\"acceptedContext\":\"function reviewedSymbol() {\\n  await database.transaction(async (tx) => { await orderRepository.save(order, tx); await paymentRepository.save(payment, tx) })\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"architecture\",\"title\":\"Use transactions for coupled writes\",\"rule\":\"Writes that must succeed together must run in one database transaction.\",\"rationale\":\"The accepted implementation wraps the order and payment writes in a shared transaction.\",\"detection\":{\"mode\":\"missing-required-signal\",\"semanticDescription\":\"Coupled persistence writes occur without a shared transaction.\",\"triggerSignals\":[\"orderRepository.save\",\"paymentRepository.save\"],\"forbiddenSignals\":[],\"requiredSignals\":[\"database.transaction\"],\"matchScope\":\"file\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/platform\",\"pullRequest\":70,\"filePath\":\"src/jobs/expiry.test.ts\",\"reviewComment\":\"Mock the clock so this test doesn't depend on wall time.\",\"rejectedCode\":\"expect(isExpired(new Date())).toBe(false)\",\"acceptedCode\":\"vi.setSystemTime(fixedNow); expect(isExpired(new Date())).toBe(false)\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  expect(isExpired(new Date())).toBe(false)\\n}\",\"acceptedContext\":\"function reviewedSymbol() {\\n  vi.setSystemTime(fixedNow); expect(isExpired(new Date())).toBe(false)\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"testing\",\"title\":\"Control time in unit tests\",\"rule\":\"Time-dependent unit tests must use a fixed mocked clock.\",\"rationale\":\"The accepted test fixes system time before evaluating expiration behavior.\",\"detection\":{\"mode\":\"missing-required-signal\",\"semanticDescription\":\"A time-dependent test uses wall time without fixing the clock.\",\"triggerSignals\":[\"isExpired\"],\"forbiddenSignals\":[],\"requiredSignals\":[\"vi.setSystemTime\"],\"matchScope\":\"file\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/web\",\"pullRequest\":139,\"filePath\":\"src/routes/settings.ts\",\"reviewComment\":\"State-changing browser routes require CSRF protection.\",\"rejectedCode\":\"router.post('/settings', updateSettings)\",\"acceptedCode\":\"router.post('/settings', verifyCsrf, updateSettings)\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  router.post('/settings', updateSettings)\\n}\",\"acceptedContext\":\"function reviewedSymbol() {\\n  router.post('/settings', verifyCsrf, updateSettings)\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"security\",\"title\":\"Protect state-changing routes from CSRF\",\"rule\":\"State-changing browser routes must apply CSRF verification.\",\"rationale\":\"The accepted route adds verifyCsrf before the settings handler.\",\"detection\":{\"mode\":\"missing-required-signal\",\"semanticDescription\":\"A state-changing route is defined without CSRF verification.\",\"triggerSignals\":[\"updateSettings\"],\"forbiddenSignals\":[],\"requiredSignals\":[\"verifyCsrf\"],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/web\",\"pullRequest\":145,\"filePath\":\"src/hooks/useOrders.ts\",\"reviewComment\":\"Boolean names should read as predicates; rename `loading` to `isLoading`.\",\"rejectedCode\":\"const loading = query.status === 'loading'\",\"acceptedCode\":\"const isLoading = query.status === 'loading'\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  const loading = query.status === 'loading'\\n}\",\"acceptedContext\":\"function reviewedSymbol() {\\n  const isLoading = query.status === 'loading'\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"style\",\"title\":\"Name booleans as predicates\",\"rule\":\"Boolean variables should use predicate-style names such as isLoading.\",\"rationale\":\"The accepted code renames the boolean to communicate its predicate meaning.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"A boolean variable does not use a predicate-style name.\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"acme/api\",\"pullRequest\":325,\"filePath\":\"src/lib/retry.ts\",\"reviewComment\":\"What happens when all retry attempts fail?\",\"rejectedCode\":\"return retry(operation, 3)\",\"acceptedCode\":\"\",\"codeContext\":{\"source\":\"historical-file\",\"language\":\"typescript\",\"commentLine\":2,\"enclosingSymbol\":{\"name\":\"reviewedSymbol\",\"kind\":\"function\",\"startLine\":1,\"endLine\":3},\"imports\":[],\"reviewedContext\":\"function reviewedSymbol() {\\n  return retry(operation, 3)\\n}\",\"truncated\":false}}}","output":"{\"intent\":\"question-nonactionable\",\"title\":\"Question about exhausted retries\",\"rule\":\"The reviewer is asking what happens after retry attempts are exhausted.\",\"rationale\":\"The comment asks for clarification and provides no accepted implementation change.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"The reviewer is asking what happens after retry attempts are exhausted.\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":12,\"filePath\":\"src/routes/incidents.ts\",\"reviewComment\":\"Can we also set a `Location` header pointing at the new incident resource when returning 201?\",\"rejectedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\nimport type { IncidentStore } from \\\"../store/incident-store.js\\\";\\n\\nconst createIncident = z.object({\\n  title: z.string().trim().min(3).max(120),\\n  severity: z.enum([\\\"sev1\\\", \\\"sev2\\\", \\\"sev3\\\"])\\n});\\n\\nexport function registerIncidentRoutes(app: FastifyInstance, store: IncidentStore, service: IncidentService): void {\\n  app.get(\\\"/api/incidents\\\", async () => ({ incidents: store.list() }));\\n  app.post(\\\"/api/incidents\\\", async (request, reply) => {\\n    const input = createIncident.parse(request.body);\\n    const incident = service.create(input.title, input.severity);\\n    return reply.code(201).send(incident);\",\"acceptedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\nimport type { IncidentStore } from \\\"../store/incident-store.js\\\";\\nconst createIncident = z.object({\\n  title: z.string().trim().min(3).max(120),\\n  severity: z.enum([\\\"sev1\\\", \\\"sev2\\\", \\\"sev3\\\"])\\nexport function registerIncidentRoutes(app: FastifyInstance, store: IncidentStore, service: IncidentService): void {\\n  app.get(\\\"/api/incidents\\\", async () => ({ incidents: store.list() }));\\n  app.post(\\\"/api/incidents\\\", async (request, reply) => {\\n    const input = createIncident.parse(request.body);\\n    const incident = service.create(input.title, input.severity);\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"Can we also set a `Location` header pointing at the new incident resource when r\",\"rule\":\"Can we also set a `Location` header pointing at the new incident resource when returning 201?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Can we also set a `Location` header pointing at the new incident resource when returning 201?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":8,\"filePath\":\"src/services/incident-service.ts\",\"reviewComment\":\"A domain-specific `IncidentNotFoundError` would avoid coupling the HTTP layer to the wording of this message.\",\"rejectedCode\":\"import { randomUUID } from \\\"node:crypto\\\";\\nimport type { Incident, IncidentSeverity, IncidentStatus } from \\\"../domain/incident.js\\\";\\nimport { IncidentStore } from \\\"../store/incident-store.js\\\";\\n\\nexport class IncidentService {\\n  constructor(private readonly store: IncidentStore) {}\\n\\n  create(title: string, severity: IncidentSeverity): Incident {\\n    const now = new Date().toISOString();\\n    return this.store.save({\\n      id: randomUUID(), title, severity, status: \\\"investigating\\\",\\n      createdAt: now, updatedAt: now, notes: []\\n    });\\n  }\\n\\n  transition(id: string, status: IncidentStatus): Incident {\\n    const incident = this.requireIncident(id);\\n    return this.store.save({ ...incident, status, updatedAt: new Date().toISOString() });\\n  }\\n\\n  requireIncident(id: string): Incident {\\n    const incident = this.store.get(id);\\n    if (!incident) throw new Error(`Incident ${id} was not found`);\",\"acceptedCode\":\"import { randomUUID } from \\\"node:crypto\\\";\\nimport type { Incident, IncidentSeverity, IncidentStatus } from \\\"../domain/incident.js\\\";\\nimport { IncidentStore } from \\\"../store/incident-store.js\\\";\\nexport class IncidentService {\\n  constructor(private readonly store: IncidentStore) {}\\n  create(title: string, severity: IncidentSeverity): Incident {\\n    const now = new Date().toISOString();\\n    return this.store.save({\\n      id: randomUUID(), title, severity, status: \\\"investigating\\\",\\n      createdAt: now, updatedAt: now, notes: []\\n  transition(id: string, status: IncidentStatus): Incident {\\n    const incident = this.requireIncident(id);\"}}","output":"{\"intent\":\"architecture\",\"title\":\"A domain-specific `IncidentNotFoundError` would avoid coupling the HTTP layer to\",\"rule\":\"A domain-specific `IncidentNotFoundError` would avoid coupling the HTTP layer to the wording of this message.\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"A domain-specific `IncidentNotFoundError` would avoid coupling the HTTP layer to the wording of this message.\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":13,\"filePath\":\"src/routes/incident-detail.ts\",\"reviewComment\":\"Would a transition-specific endpoint better communicate that this operation has lifecycle rules, or is PATCH preferred for client simplicity?\",\"rejectedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\n\\nconst parameters = z.object({ id: z.string().uuid() });\\nconst transition = z.object({ status: z.enum([\\\"investigating\\\", \\\"mitigating\\\", \\\"resolved\\\"]) });\\n\\nexport function registerIncidentDetailRoutes(app: FastifyInstance, service: IncidentService): void {\\n  app.get(\\\"/api/incidents/:id\\\", async request => {\\n    const { id } = parameters.parse(request.params);\\n    return service.requireIncident(id);\\n  });\\n\\n  app.patch(\\\"/api/incidents/:id/status\\\", async request => {\",\"acceptedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\nconst parameters = z.object({ id: z.string().uuid() });\\nconst transition = z.object({ status: z.enum([\\\"investigating\\\", \\\"mitigating\\\", \\\"resolved\\\"]) });\\nexport function registerIncidentDetailRoutes(app: FastifyInstance, service: IncidentService): void {\\n  app.get(\\\"/api/incidents/:id\\\", async request => {\\n    const { id } = parameters.parse(request.params);\\n    return service.requireIncident(id);\\n  app.patch(\\\"/api/incidents/:id/status\\\", async request => {\\n    const { id } = parameters.parse(request.params);\\n    const { status } = transition.parse(request.body);\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"Would a transition-specific endpoint better communicate that this operation has \",\"rule\":\"Would a transition-specific endpoint better communicate that this operation has lifecycle rules, or is PATCH preferred for client simplicity?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Would a transition-specific endpoint better communicate that this operation has lifecycle rules, or is PATCH preferred for client simplicity?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":2,\"filePath\":\"README.md\",\"reviewComment\":\"Would an example health-check request help contributors confirm that their local server started successfully?\",\"rejectedCode\":\"# SignalDesk\\n\\nSignalDesk is a TypeScript service for coordinating operational incidents.\\nIt provides a small HTTP API for incidents, responder notes, and service health.\\n\\n## Development\\n\\n```bash\\nnpm install\\nnpm run dev\\nnpm test\\n```\\n\\nThe service listens on `PORT` (default `3000`) and stores data in memory.\",\"acceptedCode\":\"# SignalDesk\\nSignalDesk is a TypeScript service for coordinating operational incidents.\\nIt provides a small HTTP API for incidents, responder notes, and service health.\\n## Development\\n```bash\\nnpm install\\nnpm run dev\\nnpm test\\nThe service listens on `PORT` (default `3000`) and stores data in memory.\\nSee `docs/api.md` for endpoints and `docs/operations.md` for deployment guidance.\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"Would an example health-check request help contributors confirm that their local\",\"rule\":\"Would an example health-check request help contributors confirm that their local server started successfully?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Would an example health-check request help contributors confirm that their local server started successfully?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":6,\"filePath\":\"src/config.ts\",\"reviewComment\":\"Nice range validation. Can we add a configuration test for non-numeric values so the startup failure remains understandable?\",\"rejectedCode\":\"import { z } from \\\"zod\\\";\\n\\nconst environmentSchema = z.object({\\n  HOST: z.string().default(\\\"0.0.0.0\\\"),\\n  PORT: z.coerce.number().int().positive().max(65535).default(3000),\",\"acceptedCode\":\"import { z } from \\\"zod\\\";\\nconst environmentSchema = z.object({\\n  HOST: z.string().default(\\\"0.0.0.0\\\"),\\n  PORT: z.coerce.number().int().positive().max(65535).default(3000),\\n  LOG_LEVEL: z.enum([\\\"debug\\\", \\\"info\\\", \\\"warn\\\", \\\"error\\\"]).default(\\\"info\\\")\\nexport type ServiceConfig = z.infer<typeof environmentSchema>;\\n  return environmentSchema.parse(environment);\"}}","output":"{\"intent\":\"testing\",\"title\":\"Nice range validation\",\"rule\":\"Nice range validation. Can we add a configuration test for non-numeric values so the startup failure remains understandable?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Nice range validation. Can we add a configuration test for non-numeric values so the startup failure remains understandable?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":19,\"filePath\":\"tests/incident-service.test.ts\",\"reviewComment\":\"Would asserting the requested title and severity here make the test cover input preservation as well as defaults?\",\"rejectedCode\":\"import { describe, expect, it } from \\\"vitest\\\";\\nimport { IncidentService } from \\\"../src/services/incident-service.js\\\";\\nimport { IncidentStore } from \\\"../src/store/incident-store.js\\\";\\n\\ndescribe(\\\"IncidentService\\\", () => {\\n  it(\\\"creates incidents in the investigating state\\\", () => {\\n    const service = new IncidentService(new IncidentStore());\\n    const incident = service.create(\\\"Elevated checkout errors\\\", \\\"sev2\\\");\\n    expect(incident.status).toBe(\\\"investigating\\\");\\n    expect(incident.notes).toEqual([]);\",\"acceptedCode\":\"import { describe, expect, it } from \\\"vitest\\\";\\nimport { IncidentService } from \\\"../src/services/incident-service.js\\\";\\nimport { IncidentStore } from \\\"../src/store/incident-store.js\\\";\\ndescribe(\\\"IncidentService\\\", () => {\\n  it(\\\"creates incidents in the investigating state\\\", () => {\\n    const service = new IncidentService(new IncidentStore());\\n    const incident = service.create(\\\"Elevated checkout errors\\\", \\\"sev2\\\");\\n    expect(incident.status).toBe(\\\"investigating\\\");\\n    expect(incident.notes).toEqual([]);\\n  it(\\\"transitions an existing incident\\\", () => {\\n    const service = new IncidentService(new IncidentStore());\\n    const incident = service.create(\\\"Queue backlog\\\", \\\"sev3\\\");\"}}","output":"{\"intent\":\"testing\",\"title\":\"Would asserting the requested title and severity here make the test cover input \",\"rule\":\"Would asserting the requested title and severity here make the test cover input preservation as well as defaults?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Would asserting the requested title and severity here make the test cover input preservation as well as defaults?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":20,\"filePath\":\"tests/api.test.ts\",\"reviewComment\":\"Can the health test also check the content type and request-ID header so the operational contract is covered?\",\"rejectedCode\":\"import { afterEach, describe, expect, it } from \\\"vitest\\\";\\nimport { buildApp } from \\\"../src/app.js\\\";\\n\\ndescribe(\\\"SignalDesk API\\\", () => {\\n  const app = buildApp();\\n  afterEach(async () => app.close());\\n\\n  it(\\\"reports service health\\\", async () => {\\n    const response = await app.inject({ method: \\\"GET\\\", url: \\\"/health\\\" });\\n    expect(response.statusCode).toBe(200);\\n    expect(response.json()).toMatchObject({ status: \\\"ok\\\", service: \\\"signaldesk\\\" });\",\"acceptedCode\":\"import { afterEach, describe, expect, it } from \\\"vitest\\\";\\nimport { buildApp } from \\\"../src/app.js\\\";\\ndescribe(\\\"SignalDesk API\\\", () => {\\n  const app = buildApp();\\n  afterEach(async () => app.close());\\n  it(\\\"reports service health\\\", async () => {\\n    const response = await app.inject({ method: \\\"GET\\\", url: \\\"/health\\\" });\\n    expect(response.statusCode).toBe(200);\\n    expect(response.json()).toMatchObject({ status: \\\"ok\\\", service: \\\"signaldesk\\\" });\\n  it(\\\"creates an incident\\\", async () => {\\n    const response = await app.inject({ method: \\\"POST\\\", url: \\\"/api/incidents\\\", payload: { title: \\\"Search latency\\\", severity: \\\"sev2\\\" } });\\n    expect(response.statusCode).toBe(201);\"}}","output":"{\"intent\":\"testing\",\"title\":\"Can the health test also check the content type and request-ID header so the ope\",\"rule\":\"Can the health test also check the content type and request-ID header so the operational contract is covered?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Can the health test also check the content type and request-ID header so the operational contract is covered?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":14,\"filePath\":\"src/routes/notes.ts\",\"reviewComment\":\"Should resolved incidents still accept notes for post-incident context, or should this endpoint enforce an active-state rule?\",\"rejectedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\nimport type { NoteService } from \\\"../services/note-service.js\\\";\\n\\nconst parameters = z.object({ id: z.string().uuid() });\\nconst noteInput = z.object({ author: z.string().min(2), message: z.string().min(1).max(1000) });\\n\\nexport function registerNoteRoutes(app: FastifyInstance, incidents: IncidentService, notes: NoteService): void {\\n  app.post(\\\"/api/incidents/:id/notes\\\", async (request, reply) => {\",\"acceptedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nimport { z } from \\\"zod\\\";\\nimport type { IncidentService } from \\\"../services/incident-service.js\\\";\\nimport type { NoteService } from \\\"../services/note-service.js\\\";\\nconst parameters = z.object({ id: z.string().uuid() });\\nconst noteInput = z.object({ author: z.string().min(2), message: z.string().min(1).max(1000) });\\nexport function registerNoteRoutes(app: FastifyInstance, incidents: IncidentService, notes: NoteService): void {\\n  app.post(\\\"/api/incidents/:id/notes\\\", async (request, reply) => {\\n    const { id } = parameters.parse(request.params);\\n    const input = noteInput.parse(request.body);\\n    const incident = notes.add(incidents.requireIncident(id), input.author, input.message);\\n    return reply.code(201).send(incident);\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"Should resolved incidents still accept notes for post-incident context, or shoul\",\"rule\":\"Should resolved incidents still accept notes for post-incident context, or should this endpoint enforce an active-state rule?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Should resolved incidents still accept notes for post-incident context, or should this endpoint enforce an active-state rule?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":21,\"filePath\":\"Dockerfile\",\"reviewComment\":\"This requires a committed lockfile. Can we ensure the package initialization work includes one before relying on `npm ci`?\",\"rejectedCode\":\"FROM node:22-alpine AS build\\nWORKDIR /app\\nCOPY package*.json ./\\nRUN npm ci\",\"acceptedCode\":\"FROM node:22-alpine AS build\\nWORKDIR /app\\nCOPY package*.json ./\\nRUN npm ci\\nCOPY tsconfig.json ./\\nCOPY src ./src\\nRUN npm run build\\nFROM node:22-alpine AS runtime\\nWORKDIR /app\\nCOPY --from=build /app/package*.json ./\\nCOPY --from=build /app/node_modules ./node_modules\\nCOPY --from=build /app/dist ./dist\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"This requires a committed lockfile\",\"rule\":\"This requires a committed lockfile. Can we ensure the package initialization work includes one before relying on `npm ci`?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"This requires a committed lockfile. Can we ensure the package initialization work includes one before relying on `npm ci`?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
{"input":"{\"task\":\"analyze_review_episode\",\"version\":\"2\",\"instruction\":\"Normalize one pull-request review episode into the Engineering Memory SemanticAnalysis v2 schema. Use only the supplied evidence and return raw JSON without Markdown. Return exactly: intent, title, rule, rationale, detection. intent must be one of actionable-change, architecture, testing, security, style, question-nonactionable. Signals must be exact code substrings, never English descriptions. detection contains mode, semanticDescription, triggerSignals, forbiddenSignals, requiredSignals, matchScope. Use forbidden-signal when code is disallowed in the trigger context, missing-required-signal when the trigger requires absent code, and semantic only when deterministic signals cannot represent the condition. Use codeContext to understand the enclosing symbol and imports, but do not invent repository facts. When acceptedCode adds a guard, middleware, wrapper, modifier, await, mock, or helper missing from rejectedCode, use missing-required-signal; keep forbiddenSignals empty and never mark the trigger itself forbidden. The application derives legacy preferredSignals and prohibitedSignals; do not return them. Use forbidden-signal only when acceptedCode removes or replaces a disallowed construct. Prefer the smallest reusable exact substring over an entire code line. Treat feature flags as architecture/release-control conventions.\",\"episode\":{\"repository\":\"chene0/ht6-dummy-repo\",\"pullRequest\":16,\"filePath\":\"src/http/request-context.ts\",\"reviewComment\":\"Should we accept a trusted incoming request ID, or always generate our own to avoid unbounded and spoofed correlation values?\",\"rejectedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\n\\nexport function registerRequestContext(app: FastifyInstance): void {\\n  app.addHook(\\\"onSend\\\", async (request, reply, payload) => {\\n    reply.header(\\\"x-request-id\\\", request.id);\",\"acceptedCode\":\"import type { FastifyInstance } from \\\"fastify\\\";\\nexport function registerRequestContext(app: FastifyInstance): void {\\n  app.addHook(\\\"onSend\\\", async (request, reply, payload) => {\\n    reply.header(\\\"x-request-id\\\", request.id);\\n    reply.header(\\\"cache-control\\\", \\\"no-store\\\");\\n    return payload;\"}}","output":"{\"intent\":\"actionable-change\",\"title\":\"Should we accept a trusted incoming request ID, or always generate our own to av\",\"rule\":\"Should we accept a trusted incoming request ID, or always generate our own to avoid unbounded and spoofed correlation values?\",\"rationale\":\"The linked merged code does not isolate a safe exact signal, so this review remains semantic-only.\",\"detection\":{\"mode\":\"semantic\",\"semanticDescription\":\"Should we accept a trusted incoming request ID, or always generate our own to avoid unbounded and spoofed correlation values?\",\"triggerSignals\":[],\"forbiddenSignals\":[],\"requiredSignals\":[],\"matchScope\":\"line\"}}"}
